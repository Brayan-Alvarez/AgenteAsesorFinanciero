# Frontend environment variables — copy to .env and fill in values.
# In production (Vercel), set these in the Vercel dashboard instead.

# URL of the deployed FastAPI backend.
# Local dev: http://localhost:8000
# Production: https://your-backend.railway.app  (or Render URL)
VITE_API_URL=http://localhost:8000
